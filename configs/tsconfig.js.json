{
    "extends": "./tsconfig.base.json",
    "compilerOptions": {
        "composite": true,
        "lib": ["ESNext", "DOM"],

        /* Bundler mode */
        "jsx": "preserve",
        "jsxImportSource": "solid-js",
        "types": ["vite/client"]
    },
    "include": ["../web"],
    "exclude": ["../**/*.worker.ts"]
}
